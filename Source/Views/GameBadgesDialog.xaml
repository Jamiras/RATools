<UserControl x:Class="RATools.Views.GameBadgesDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="812" Height="Auto" MinHeight="113">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Jamiras.UI.WPF;component/Controls/Styles/ProgressOverlay.xaml" />
                <ResourceDictionary Source="/Jamiras.UI.WPF;component/Controls/Styles/SubtleHyperlink.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Game ID:" VerticalAlignment="Center" Margin="8,0,0,0" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding GameId}" Height="20" Margin="4,2,4,0" HorizontalContentAlignment="Right" />
        <Button Grid.Row="0" Grid.Column="2" Command="{Binding SearchCommand}" Content="Search" Width="60" Margin="2,5,0,4" />
        <CheckBox Grid.Row="0" Grid.Column="3" x:Name="showHardcoreBorderCheckBox"
                  IsChecked="{Binding ShowHardcoreBorder}" Content="Hardcore Border" Margin="40,7,0,4" />

        <ItemsControl Grid.Row="1" Grid.ColumnSpan="5" ItemsSource="{Binding Achievements}"
                      Background="#101012" Margin="4" Padding="8">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="2">
                        <Border.Style>
                            <Style TargetType="{x:Type Border}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=showHardcoreBorderCheckBox, Path=IsChecked}" Value="True">
                                        <Setter Property="BorderBrush" Value="#FFD700" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Image Source="{Binding Badge}" Width="48" Height="48" />
                        <Border.ToolTip>
                            <ToolTip Background="#101012" BorderBrush="#2A2A2A">
                                <Grid Margin="0,2,4,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Image Source="{Binding Badge}" Width="64" Height="64" />
                                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                        <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" Foreground="#C8C8C8" />
                                        <TextBlock Text="{Binding Description}" Margin="0,4,0,4" FontSize="14" Foreground="#C8C8C8" />
                                    </StackPanel>
                                </Grid>
                            </ToolTip>
                        </Border.ToolTip>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ContentPresenter Grid.ColumnSpan="4" Grid.RowSpan="2" Margin="-4" Content="{Binding Progress}" />
    </Grid>
</UserControl>
