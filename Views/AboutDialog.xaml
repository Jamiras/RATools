<UserControl x:Class="RATools.Views.AboutDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RATools.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--<Image />-->

        <TextBlock Grid.Column="1">
            <TextBlock  FontWeight="Bold" Text="{Binding ProductVersion}" />
            <Run Text=" - " />
            <TextBlock Text="{Binding BuildDate}" />
        </TextBlock>
        <!--<TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding CopyrightMessage}" />-->
        <TextBlock Grid.Column="1" Grid.Row="2">
            <Hyperlink Command="{Binding SourceLinkCommand}">
                <TextBlock Text="{Binding SourceLink}" />
            </Hyperlink>
        </TextBlock>
    </Grid>
</UserControl>
